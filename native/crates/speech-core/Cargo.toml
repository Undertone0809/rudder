[package]
name = "rudder-speech-core"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
whisper-rs = "0.16.0"

[dev-dependencies]
serde_json = "1.0"
