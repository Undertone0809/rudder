[package]
name = "rudder-plugin-archive-core"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
base64 = "0.22"
serde = { version = "1.0", features = ["derive"] }
zip = { version = "4.6", default-features = false, features = ["deflate"] }

[dev-dependencies]
serde_json = "1.0"
tempfile = "3.23"
