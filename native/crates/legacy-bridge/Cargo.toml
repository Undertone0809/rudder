[package]
name = "rudder-legacy-bridge"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
rudder-authority-core = { path = "../authority-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3.23"
