[package]
name = "rudder-cli-mcp-contract-core"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
rudder-workspace-manifest-core = { path = "../workspace-manifest-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.23"
