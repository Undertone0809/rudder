[package]
name = "rudder-mcp"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
rudder-cli-mcp-contract-core = { path = "../../crates/cli-mcp-contract-core" }
serde_json = "1.0"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3.23"
