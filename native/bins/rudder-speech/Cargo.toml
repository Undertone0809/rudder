[package]
name = "rudder-speech"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
rudder-speech-core = { path = "../../crates/speech-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
