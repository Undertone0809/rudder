[package]
name = "rudder-native"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
rudder-archive-core = { path = "../../crates/archive-core" }
rudder-run-evidence-core = { path = "../../crates/run-evidence-core" }
rudder-workspace-manifest-core = { path = "../../crates/workspace-manifest-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
crc32fast = "1.4"
serde_json = "1.0"
tempfile = "3.23"
