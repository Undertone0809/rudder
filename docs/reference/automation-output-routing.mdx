---
title: "Automation output routing"
description: "Compare tracked Issue and Chat output for Rudder Automations, including run records and failure evidence."
icon: "route"
keywords:
  - "Automation output routing"
  - "Rudder Automation Issue output"
  - "Rudder Automation Chat output"
canonical: "https://docs.rudderhq.dev/reference/automation-output-routing"
"og:url": "https://docs.rudderhq.dev/reference/automation-output-routing"
hreflang_en: "https://docs.rudderhq.dev/reference/automation-output-routing"
hreflang_zh: "https://docs.rudderhq.dev/zh/reference/automation-output-routing"
---

<a id="definition" />

## Definition

Automation output routing decides which work surface owns each execution
result. Custom Automations default to Chat output. Tracked Issue output remains
an explicit choice when repeated work needs Issue fields and lifecycle state.

<a id="states" />

## States and modes

| Output mode | Result of a real execution | Source link on the Automation Run |
| --- | --- | --- |
| `chat_output` | A new Messenger conversation for that execution | `linkedChatConversationId` |
| `track_issue` | A new execution Issue that enters the normal assignment and review path | `linkedIssueId` |

An Automation Run records whether dispatch was received, started, completed,
failed, skipped, or coalesced. The exact terminal state remains on the Run even
when no new Chat or Issue is created.

<a id="constraints" />

## Constraints

- Every real Chat-output execution gets its own conversation. Rudder does not
  reuse an arbitrary human Chat as a permanent output sink.
- Skipped or coalesced Runs do not create empty conversations.
- A failed Chat-output Run keeps visible partial or fallback evidence.
- Tracked Issue output follows normal assignment, status, Agent Run, Review,
  and close-out rules after the Issue is created.

<a id="boundaries" />

## Boundaries

Output mode does not decide whether work is important, expensive, or
long-running. Choose the surface based on the coordination record people need.
A governed policy may still require an Issue even when the result could be read
in Chat.

The Automation Run remains the dispatch record in both modes. A Chat transcript
does not replace the Run, and an execution Issue does not replace the
Automation definition or schedule.

<a id="examples" />

## Examples

- Send a daily digest to Chat when operators will read it and ask follow-up
  questions in the resulting conversation.
- Track a weekly compliance check as an Issue when it needs an assignee,
  acceptance criteria, artifacts, and a reviewer decision.
- If concurrency policy coalesces a duplicate tick, inspect the Automation Run
  for the reason. Do not expect an empty Chat or Issue placeholder.

<CardGroup cols={2}>
  <Card title="Create an Automation" icon="workflow" href="/how-to/create-automation">
    Configure the trigger, owner, context, and output mode.
  </Card>
  <Card title="Issue statuses" icon="list-checks" href="/reference/issue-statuses">
    Check the lifecycle used by tracked Issue output.
  </Card>
</CardGroup>
