---
title: "管理 Workspaces 和 Library"
description: "把项目资源、Library files、持久输出、backups 和 run workspaces 放在正确位置。"
icon: "folder-tree"
keywords:
  - "Rudder Library"
  - "agent workspace"
  - "project resources"
canonical: "https://docs.rudderhq.dev/zh/how-to/manage-workspaces-and-library"
"og:url": "https://docs.rudderhq.dev/zh/how-to/manage-workspaces-and-library"

---

Workspaces 和 Library 的作用是让 agent context 和输出可查找。未来的 run 应该知道去哪里读，人也应该知道哪些文件是持久产物。

## 选择正确位置

| 内容 | 放到这里 |
| --- | --- |
| 项目仓库工作 | Project workspace |
| 持久报告、计划、截图、handoff | Library 里的项目文件夹 |
| 共享参考资料 | Library files |
| 精选 run context | Project Context resources |
| Agent 私有 memory 和 skills | Agent home |
| 临时文件 | `/tmp` 或系统临时目录 |

## 添加 Library files

Library 适合保存应该留在组织记录里的文件。Library files 可以被 issues、resources 和 agent context 引用。

适合放进 Library 的文件包括：

- research brief
- decision note
- source document
- reusable checklist
- project report
- handoff packet

临时截图、local cache 或 scratch files 不要放进 Library，除非它们是最终 evidence 的一部分。

## 绑定 project resources

Project Context resources 是 project-linked runs 的起始上下文。把 agent 默认应该看到的 files、URLs、repositories 或 connector objects 绑定进去。

不要把所有 organization resources 都绑定进去。默认 context 应该小到 agent 能真正使用。

## 用 project folder 保存持久输出

Rudder 会为每个 project 创建 Library folder。持久项目文件放在那里。Agent 可以自然增加 `research/`、`screenshots/`、`handoff/` 或 `evals/` 等子目录。

用 issue comments 或 review notes 指向 reviewer 需要看的文件。

## 在当前工作旁边检查和编辑文件

从 Chat、Messenger 或 issue 打开 Library 引用时，可以先在 Side Panel 中检查，不必离开当前工作面。PDF 等支持的文件会直接内联预览。

编辑 Markdown 文件时：

1. 直接在 Side Panel 中修改，并留意 save state。
2. 如果 Rudder 提示 stale-write conflict，先检查仍然保留的本地草稿。
3. 选择 **Keep mine**，用你的草稿基于最新版本重试；或者选择 **Use latest**，换成 server 当前版本。

不要把 conflict 当作已经保存成功。只有 guarded save 成功，或者你明确选择 latest version 后，这个本地草稿才算处理完。

在 Desktop 里，可以通过文件 **Open** 菜单用系统默认应用或检测到的 IDE 打开文件，在文件管理器中定位它，在 terminal 中打开所在目录，或者用 **Open in Library** 进入完整 Library 工作面。

## 谨慎使用 backups

Workspace backups 是 operator 的恢复和检查工具：

- restore 前先浏览 backup versions
- 只有理解会改变什么时才 restore
- 旧 backup 没用了就删除
- 需要在 Rudder 外检查或保存时再 download

## 下一步

<CardGroup cols={2}>
  <Card title="工作区" icon="folder-tree" href="/zh/concepts/workspaces">
    理解 workspace boundaries。
  </Card>
  <Card title="Workspace boundaries" icon="list-checks" href="/zh/reference/workspace-boundaries">
    检查文件应该放在哪里。
  </Card>
</CardGroup>
