---
title: "Quick Start"
description: "Install Rudder Desktop and the Rudder CLI, then start the local app for AI agent work management."
icon: "zap"
keywords:
  - "install Rudder"
  - "Rudder CLI"
  - "AI agent desktop app"
  - "local agent work management"
canonical: "https://docs.rudderhq.dev/get-started/installation"
"og:url": "https://docs.rudderhq.dev/get-started/installation"

---

The fastest path installs the per-user portable Rudder Desktop app and prepares the matching persistent CLI. This is the recommended setup when you want a local AI agent work app that can create organizations, assign issues, run agents, and keep review evidence on your machine.

## What does the install command do?

`npx @rudderhq/cli@latest start` starts Rudder's public local setup flow. It prepares the Rudder CLI, opens the portable Desktop app, and keeps the installed app aligned with the selected release channel.

![Rudder CLI terminal workflow](../images/installation-cli.png)

Run the public install command:

```bash
npx @rudderhq/cli@latest start
```

After the persistent CLI is available, the direct `rudder` form uses the same command surface.

```bash
rudder start
```

Use this path when your goal is to open the local app, create an organization,
and run agent work. The command keeps the CLI and portable Desktop app aligned
with the selected release.

## Recover from a Desktop startup failure

A healthy Desktop startup shows the Rudder identity and motion without
technical stage names or percentage progress. That quiet screen is the normal
waiting state.

If the managed local server reports a startup failure, the same window reveals
a plain-language error and recovery actions:

- **Try again** starts one fresh startup attempt in the existing window.
- **Technical details** reveals bounded diagnostics that you can copy when
  investigating the failure.
- **Email support** opens an editable draft. Rudder does not send the message,
  upload local data, or attach workspace files automatically.

Review technical details before sharing them, and do not add credentials,
databases, `.env` files, or private workspace contents to a support request.

## Install updates without losing active work

Packaged Rudder Desktop can download a verified update while Agent Runs are
still active. When active runs exist, choose deliberately:

- **Update when idle** downloads the update and keeps current runs alive until
  they finish.
- **Stop runs and update now** cancels active runs, quits Rudder, and applies the
  update immediately.
- **Cancel** leaves the current version running.

The deferred path does not cancel active work. If an update fails or expires,
Rudder keeps the failure visible and offers retry and release-page actions.

## Contributor setup

Use the repository workflow when you are developing Rudder itself.

```bash
git clone https://github.com/Undertone0809/rudder
cd rudder
pnpm install
pnpm dev
```

This starts the API server and UI at `http://localhost:3100`.

Rudder defaults to embedded PostgreSQL in development. If `DATABASE_URL` is unset, no separate database is required.

Use this path only when you are changing Rudder source code. It starts the local
development server and does not install the public portable Desktop app.

## Next steps

<CardGroup cols={1}>
  <Card title="Create Your First Organization" icon="building-2" href="/get-started/first-organization">
    Set up a goal, agents, issues, and review evidence.
  </Card>
</CardGroup>
