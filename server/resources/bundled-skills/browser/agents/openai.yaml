interface:
  display_name: "Browser"
  short_description: "Control and test pages in Rudder Browser"
  default_prompt: "Use $browser to inspect this page and interact through safe high-level refs or explicit coordinate CUA in Rudder's built-in Browser."

dependencies:
  tools:
    - type: "mcp"
      value: "rudder-browser"
      description: "Run-scoped Rudder Browser automation"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
