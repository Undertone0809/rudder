This release restores Windows Desktop installation through the default Rudder CLI path.

## Fixed

- Fixed Windows x64 installation failing while extracting the downloaded Desktop archive because `tar.exe` treated the local drive-letter path as a remote address.
