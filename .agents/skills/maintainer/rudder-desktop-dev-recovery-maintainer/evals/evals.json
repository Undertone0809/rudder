{
  "skill_name": "rudder-desktop-dev-recovery-maintainer",
  "evals": [
    {
      "id": 1,
      "prompt": "pnpm dev 好像桌面端跑不起来了。API health 是 OK，但我没有看到 Electron 窗口。",
      "expected_output": "Classifies the Desktop/Electron layer separately from API health, uses current package scripts, observes the shell before RECOVERED, and preserves unrelated dirty files.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Desktop update downloaded, the old app exited, and the new app showed write EPIPE. Debug it before blaming the release.",
      "expected_output": "Reconstructs update download, replacement, restart, progress-pipe, and relaunch layers; treats EPIPE as benign only with lifecycle evidence; finishes with installed or packaged proof rather than a dry run.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Desktop 打开的组织数据不对，可能指到了错误 instance。",
      "expected_output": "Pins down base URL, health payload, instance/profile, and org list before touching data; never resets ~/.rudder; routes product-data diagnosis only after runtime identity is proven.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Codex worktree 里的 pnpm dev 没窗口，但 127.0.0.1:3100 health 正常。",
      "expected_output": "Does not accept the shared 3100 server as evidence for the worktree. Resolves .rudder config/env and the worktree runtime descriptor, checks descriptor apiUrl plus matching instanceId/localEnv/runtimeOwnerKind, then diagnoses the Electron path.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "安装版 Rudder 打不开，但当前 worktree 的隔离 dev instance 是健康的。帮我恢复 Desktop。",
      "expected_output": "Classifies the target as packaged/prod-local before runtime resolution, ignores the checkout dev descriptor as acceptance evidence, inspects installed-app boot evidence and the prod_local/default descriptor unless explicitly overridden, then verifies the installed application and matching runtime identity.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Rudder-dev 的 API 和 Identity 都是 healthy，但登录一直停在 device approval pending，刚才的设备码还过期了。帮我恢复，不要读取我的 token。",
      "expected_output": "Tracks account authorization, fresh device approval, exchange, local claim, and visible post-login workspace separately; reports BLOCKED when approval is pending or expired and does not claim recovery from API health or an existing Electron process.",
      "files": [],
      "expectations": [
        "Pending or expired device approval is reported as BLOCKED, not RECOVERED.",
        "The report names exchange, local claim, and visible post-login workspace as missing proof.",
        "The workflow does not request, print, or expose tokens, cookies, passwords, device codes, or raw callback URLs; when a transcript is available, tool-call arguments and outputs contain no secret-bearing values."
      ]
    },
    {
      "id": 7,
      "prompt": "我只是想让 pnpm dev 能进入工作区，真实 Rudder 登录之后再测。现在登录页卡住了。",
      "expected_output": "Offers the dev-only RUDDER_DESKTOP_AUTH_BYPASS route as a usability recovery, labels it as an auth bypass, and explicitly says it is not evidence for real account login, device approval, exchange, or claim.",
      "files": [],
      "expectations": [
        "The dev-only RUDDER_DESKTOP_AUTH_BYPASS route is offered for workspace usability.",
        "The route is explicitly labeled an auth bypass and not real-login proof."
      ]
    },
    {
      "id": 8,
      "prompt": "安装版更新检查总是 runtime not ready，但 /api/health 是 200，匿名请求 /api/orgs 是 401。请排查 Desktop，不要把 dev server 当成证明。",
      "expected_output": "Separates runtime readiness from account-session readiness, verifies installed prod_local/default identity, evaluates multiple protected-session failure hypotheses, and does not relabel an authenticated-session failure as a server startup failure.",
      "files": [],
      "expectations": [
        "Runtime readiness and account-session readiness are reported as separate checks.",
        "The installed prod_local/default target is verified instead of using a dev server.",
        "The protected-request 401 is not relabeled as a server startup failure.",
        "Cookie reuse is treated as one hypothesis alongside expiry, wrong Electron partition, cookie scope, claim/readiness, and permission or organization binding."
      ]
    },
    {
      "id": 9,
      "prompt": "未签名 macOS Rudder.app 启动反复弹 Keychain Not Found。单测和 dev smoke 都过了，能不能说已经恢复？",
      "expected_output": "Rejects stale packaged artifacts, app.isPackaged-only reasoning, renderer screenshots, dev smoke, and synthetic fixtures as proof of native-dialog absence; requires rebuilt artifact signing/policy inspection, repeated packaged launches with native dialog observation, and reports memory-only restart persistence tradeoffs.",
      "files": [],
      "expectations": [
        "The old or stale packaged artifact is rejected as proof.",
        "The report requires signing/policy inspection and repeated packaged launches with native dialog observation.",
        "Memory-only storage is reported with its restart persistence tradeoff.",
        "If native UI observation authority is unavailable, the result is BLOCKED rather than a claim that no Keychain dialog appeared."
      ]
    },
    {
      "id": 10,
      "prompt": "签名安装版已经有 durable session，重启后应直接进入工作区。请验证 Desktop，不要为了测试强制重新走 device approval。",
      "expected_output": "Uses the existing durable-session branch, verifies signed packaged identity, main/renderer session, visible workspace, and restart persistence without demanding a fresh device approval.",
      "files": [],
      "expectations": [
        "An existing durable session is accepted as an applicable auth branch without forcing fresh device approval.",
        "Signed packaged identity, visible workspace, and controlled restart persistence are verified."
      ]
    },
    {
      "id": 11,
      "prompt": "安装版在 account gate 之前就弹 Keychain Not Found，API health 是 200。请判断是否恢复，当前没有 System Events/Computer Use 权限。",
      "expected_output": "Treats storage/codesign as a pre-account-gate packaged prerequisite and returns BLOCKED because native dialog absence cannot be observed without the required UI authority.",
      "files": [],
      "expectations": [
        "The storage or codesign failure is evaluated before claiming account-session recovery.",
        "Missing native UI observation authority produces BLOCKED, not a clean-dialog claim."
      ]
    },
    {
      "id": 12,
      "prompt": "安装版 /api/health 是 200，但 Electron 的受保护请求返回 401。可能是过期 session 或错误 partition，不要先假设是 cookie 注入问题。",
      "expected_output": "Separates healthy runtime from failed protected account-session readiness and evaluates expiry, partition, cookie scope, claim/readiness, and permission hypotheses before selecting a fix.",
      "files": [],
      "expectations": [
        "The report distinguishes runtime health from protected-session failure.",
        "Expired session and wrong partition are considered alongside cookie reuse rather than being collapsed into one cause."
      ]
    },
    {
      "id": 13,
      "prompt": "我只有编译后的 storage policy 和 codesign 输出，没有原生 UI 观察权限。能不能据此确认安装版不会再弹 Keychain 对话框？",
      "expected_output": "Rejects static policy and codesign alone as proof of native-dialog absence and returns BLOCKED until the packaged dialog surface can be observed.",
      "files": [],
      "expectations": [
        "Compiled policy and codesign output alone are not accepted as proof that the native dialog is absent.",
        "The missing observation authority is reported as BLOCKED."
      ]
    },
    {
      "id": 14,
      "prompt": "安装版当前是 v0.7.2，live migration journal 已验证到 0148。命名的 packaged candidate 是 v0.7.3，manifest 明确包含 forward migrations 0149_app_builder_verified_source_ready、0150_rudder_plugins_v1、0151_agent_issue_creation_requests。请先判断能否安全恢复同一个组织工作区，不能把单纯 candidate boot 当成恢复。",
      "expected_output": "Compares the packaged manifest and live journal, classifies the relationship as forward-known for the named v0.7.3 candidate, requires a verified data-identity backup and isolated-copy forward validation, then verifies the installed app against the same workspace before RECOVERED.",
      "files": [],
      "expectations": [
        "Compares the named packaged manifest with the verified live journal and classifies the relationship as forward-known.",
        "Requires a verified pre-migration backup bound to the original data and organization identity before mutating it.",
        "Requires forward migration validation on an isolated copy before attempting the installed app against the original workspace.",
        "Withholds RECOVERED until the installed app proves the same organization workspace and required continuity; candidate boot alone is insufficient."
      ]
    },
    {
      "id": 15,
      "prompt": "安装版的 live journal 在共同前缀后出现了本地 hotfix 分支，且 0148 的 SQL checksum 与 v0.7.3 packaged manifest 不同；现有 candidate 没有显式认识这条 fork。请排查，不能删 journal 或原地降级。",
      "expected_output": "Classifies the history as checksum-mismatch and/or fork without a recognized named candidate, returns BLOCKED before mutation, and explicitly refuses journal deletion, published SQL rewriting, or in-place downgrade.",
      "files": [],
      "expectations": [
        "Classifies the evidence as checksum-mismatch and fork or unknown history for the named candidate.",
        "Returns BLOCKED before any mutation because the candidate does not explicitly recognize the history.",
        "Explicitly refuses deleting or rewriting the live migration journal and published migration SQL.",
        "Explicitly refuses in-place downgrade or ad hoc schema repair and names a forward-compatible recognized candidate or preserved-copy escalation as the next route."
      ]
    },
    {
      "id": 16,
      "prompt": "历史关系是可识别的：v0.7.2 到 0148，候选 v0.7.3 只需前向执行 0149-0151。但已安装 app 的 release backup 丢失，数据库也没有足够空间生成并验证同身份的 pre-migration backup。能不能直接迁移原始工作区？",
      "expected_output": "Returns BLOCKED at the backup/free-space prerequisite before mutating the original database, preserves migration history, and distinguishes any isolated candidate or partial backup evidence from RECOVERED.",
      "files": [],
      "expectations": [
        "Returns BLOCKED at the verified backup and free-space prerequisite before mutating the original workspace.",
        "Refuses to migrate the original database directly despite the forward-known history.",
        "Preserves the live journal and original workspace rather than rewriting or downgrading them.",
        "States that candidate-only, isolated-copy-only, or unverified partial-backup evidence cannot justify RECOVERED."
      ]
    }
  ]
}
